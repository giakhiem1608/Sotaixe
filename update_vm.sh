sed -i 's/    fun hideRevenueSource(source: RevenueSource) {/    fun deleteRevenueSource(source: RevenueSource) {\n        viewModelScope.launch {\n            if (repository.countRevenueEntries(source.id) > 0) {\n                repository.updateRevenueSource(source.copy(isActive = false))\n            } else {\n                repository.deleteRevenueSource(source)\n            }\n        }\n    }\n\n    fun hideRevenueSource(source: RevenueSource) {/g' app/src/main/java/com/example/ui/viewmodels/LedgerViewModel.kt

sed -i 's/    fun hideExpenseCategory(category: ExpenseCategory) {/    fun deleteExpenseCategory(category: ExpenseCategory) {\n        viewModelScope.launch {\n            if (repository.countExpenseEntries(category.id) > 0) {\n                repository.updateExpenseCategory(category.copy(isActive = false))\n            } else {\n                repository.deleteExpenseCategory(category)\n            }\n        }\n    }\n\n    fun hideExpenseCategory(category: ExpenseCategory) {/g' app/src/main/java/com/example/ui/viewmodels/LedgerViewModel.kt
