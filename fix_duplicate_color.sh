sed -i 's/                Spacer(modifier = Modifier.height(16.dp))/                val sources = viewModel.activeRevenueSources.collectAsState(initial = emptyList()).value\n                val isDuplicate = sources.any { it.colorHex == selectedColor && it.id != source.id }\n                if (isDuplicate) {\n                    Text("Màu này đang được dùng cho nguồn khác.", style = MaterialTheme.typography.bodySmall, color = MaterialTheme.colorScheme.error)\n                }\n                Spacer(modifier = Modifier.height(16.dp))/g' app/src/main/java/com/example/ui/screens/ManageScreens.kt

sed -i 's/                Row(modifier = Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.SpaceBetween) {/                Row(modifier = Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.SpaceBetween) {/g' app/src/main/java/com/example/ui/screens/ManageScreens.kt
