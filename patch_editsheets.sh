sed -i 's/Row(modifier = Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.spacedBy(8.dp)) {/FlowRow(modifier = Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.spacedBy(8.dp), verticalArrangement = Arrangement.spacedBy(8.dp)) {/g' app/src/main/java/com/example/ui/screens/EditSheets.kt
sed -i 's/enabled = amountStr.isNotBlank() && tripsStr.isNotBlank() && (tripsStr.toIntOrNull() ?: 0) >= 1/enabled = (amountStr.replace(Regex("[^0-9]"), "").toLongOrNull() ?: 0L) > 0L \&\& tripsStr.isNotBlank() \&\& (tripsStr.toIntOrNull() ?: 0) >= 1/g' app/src/main/java/com/example/ui/screens/EditSheets.kt
sed -i 's/enabled = amountStr.isNotBlank()/enabled = (amountStr.replace(Regex("[^0-9]"), "").toLongOrNull() ?: 0L) > 0L/g' app/src/main/java/com/example/ui/screens/EditSheets.kt
