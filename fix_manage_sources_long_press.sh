sed -i 's/\.clickable { editingSource = source }/\.combinedClickable(\n                                onClick = { editingSource = source },\n                                onLongClick = { \n                                    \/\/ Long press to just change color? Actually tap already opens Edit (which has color picker). The requirement says "LONG PRESS... mở Color Picker / Palette Sheet".\n                                }\n                            )/g' app/src/main/java/com/example/ui/screens/ManageScreens.kt
