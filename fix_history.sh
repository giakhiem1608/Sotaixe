sed -i '/Row(modifier = Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.Center) {/,/                Text(if (expanded) "Thu gọn/c \
            Row(modifier = Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.Center) {\n                TextButton(\n                    onClick = { expanded = !expanded },\n                    contentPadding = PaddingValues(0.dp),\n                    modifier = Modifier.height(32.dp)\n                ) {\n                    Text(if (expanded) "Thu gọn giao dịch" else "Xem giao dịch")' app/src/main/java/com/example/ui/screens/HistoryScreen.kt
